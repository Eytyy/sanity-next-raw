import React from 'react'

import { MediaModuleProps } from '../types'

type Props = {
  items: MediaModuleProps['items']
}
export default function MediaGrid({}: Props) {
  return <div>GalleryGrid</div>
}
